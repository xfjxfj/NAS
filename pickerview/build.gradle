apply plugin: 'com.android.library'

android {
	compileSdkVersion 26

	defaultConfig {
		minSdkVersion 14
	}

	lintOptions {
		abortOnError false
	}
}

dependencies {
	api project(path: ':wheelview')
}